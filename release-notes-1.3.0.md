#### Version Number
${version-number}

#### New Features
- **US1107289**: Added PQC TLS support for DropWizard with `X25519MLKEM768` and `SSL_JCE_PROVIDER_POLICY` (`UseBouncyCastle`, `UseBouncyCastleIfNeededForPqc` default, `UseJvmDefault`).

#### Known Issues
